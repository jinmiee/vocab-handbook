# 진미경 개발 단어장 (Vocab Handbook for Mobile)

이 프로젝트는 `단어장_진미경.xlsx` 파일을 기반으로 빌드 시점에 정적 데이터를 생성하여, 아이폰을 비롯한 모바일 기기에서 앱처럼 쾌적하게 학습할 수 있도록 제작된 **모바일 퍼스트 반응형 PWA(Progressive Web App) 단어장**입니다.

---

## 📱 주요 특징 및 기술 스택

- **프레임워크**: Next.js 14+ (App Router), React, TypeScript
- **스타일링**: Tailwind CSS (모바일 레이아웃 최적화, 넉넉한 글자 크기/여백, 3D 플립 카드 효과)
- **로컬 저장소**: 브라우저 `localStorage`를 활용하여 학습 상태(외운 단어, 헷갈려요/몰라요 대상, 최근 본 단어, 북마크, 데일리 퀴즈 성적 이력)를 관리 (Hydration 에러 차단 설계)
- **PWA (오프라인 학습 지원)**:
  - `public/sw.js`를 이용한 Stale-While-Revalidate 캐싱 전략. 오프라인 상태에서도 이미 방문했던 단어장을 끊김 없이 탐색할 수 있습니다.
  - 모바일 홈 화면 추가(Add to Home Screen) 지원 및 iOS Safari safe-area-inset-bottom(아이폰 홈 바 가려짐 방지) 및 viewport-fit 최적화 완료.

---

## 📂 프로젝트 구조

```text
vocab-handbook/
├── public/                       # PWA 에셋 및 서비스 워커
│   ├── sw.js                     # 오프라인 캐싱용 Service Worker
│   ├── icon-192.png              # 192x192 PWA 아이콘 (생성됨)
│   ├── icon-512.png              # 512x512 PWA 아이콘 (생성됨)
│   └── apple-touch-icon.png      # iOS 홈 화면 전용 고화질 아이콘
├── scripts/
│   └── parse-excel.js            # 엑셀 -> JSON 데이터 변환 스크립트
├── src/
│   ├── app/                      # Next.js App Router 화면
│   │   ├── page.tsx              # 홈 대시보드
│   │   ├── layout.tsx            # 메타데이터, PWA Viewport, iOS 최적화 및 탭 바 래퍼
│   │   ├── manifest.ts           # PWA 웹 매니페스트 생성
│   │   ├── list/                 # 검색, 카테고리/상태별 다중 필터 단어 목록
│   │   ├── vocab/[id]/           # 단어 상세 정보 (SSG 빌드)
│   │   ├── card/                 # 3D 탭/플립 암기 플래시카드
│   │   ├── quiz/                 # 4지선다 객관식 데일리 퀴즈
│   │   ├── incorrect/            # 오답노트 (오답 단어 퀴즈 탑재)
│   │   └── stats/                # 학습 성과 요약 및 시트별 진도율 통계
│   ├── components/
│   │   ├── Navigation.tsx        # 하단 고정형 모바일 앱 네비게이션 탭바
│   │   ├── ServiceWorkerRegister.tsx # Client-side 서비스 워커 등록 컴포넌트
│   │   └── VocabDetailClient.tsx # 단어 상세 화면의 Client-side 핵심 인터랙션 뷰
│   ├── data/
│   │   └── vocab.json            # 엑셀 파싱 완료된 단어장 데이터 (171개 항목)
│   └── hooks/
│       └── useVocabStore.ts      # 상태 변경 및 localStorage 영구 동기화 커스텀 훅
├── next.config.ts                # output: 'export' (완전한 정적 배포) 설정
├── tsconfig.json                 # TypeScript 설정
└── package.json                  # 프로젝트 의존성 및 스크립트 정의
```

---

## ⚡ 실행 및 빌드 방법

### 1. 개발 서버 구동
로컬에서 앱을 검토하고 수정하려면 아래 명령어를 입력합니다.
```bash
npm run dev
```
로컬 호스트 주소(기본: `http://localhost:3000`)로 모바일 크기 브라우저를 띄워 테스트할 수 있습니다.

### 2. 정적 배포 빌드
모든 페이지를 순수 정적 HTML/CSS/JS 파일로 변환하여 `out/` 폴더에 출력합니다. GitHub Pages 또는 Netlify, Vercel 정적 호스팅에 즉시 배포할 수 있는 상태가 됩니다.
```bash
npm run build
```

---

## 📊 엑셀 데이터 갱신 방법

단어 내용이 바뀌었거나 엑셀 시트에 단어를 추가한 경우 아래 절차대로 진행하세요.

1. **엑셀 파일 준비**: `단어장_진미경.xlsx` 파일을 프로젝트 루트 디렉토리에 복사해 넣습니다.
2. **데이터 변환 스크립트 실행**:
   ```bash
   npm run parse-excel
   ```
   이 스크립트는 엑셀 파일 내의 모든 시트를 순회하여 비어있지 않은 데이터를 파싱하고, 중복을 제거한 고유한 ID를 생성하여 `src/data/vocab.json`을 갱신합니다.
3. **단어 ID 생성 원리**:
   단어의 ID는 단순히 행 번호(index)가 아니라, `카테고리 + 단어(키워드)`의 조합을 MD5로 해싱한 문자열로 구성됩니다. 따라서 엑셀의 데이터 순서가 바뀌거나 행이 추가되더라도, **기존 단어의 ID가 깨지지 않아 사용자의 북마크 및 오답노트 기록이 안전하게 보존**됩니다.

---

## 📱 iPhone Safari 홈 화면에 추가하여 앱처럼 쓰는 방법

1. 아이폰의 **Safari** 브라우저를 열고 서비스가 배포된 주소에 접속합니다.
2. 하단 도구바 중앙의 **공유 버튼**(위로 향한 화살표 모양 아이콘)을 누릅니다.
3. 공유 옵션 목록에서 스크롤을 내려 **'홈 화면에 추가'**를 선택합니다.
4. 이름을 설정한 뒤 우측 상단의 **'추가'** 버튼을 누릅니다.
5. 홈 화면에 생성된 **'진미경 단어장'** 아이콘을 누르면, 브라우저 상단 주소창이 사라지고 독립된 풀스크린 네이티브 앱처럼 동작합니다.

---

## 🧪 기능 테스트 체크리스트

배포 전 아래 기능들이 올바르게 동작하는지 테스트해 보세요.

- [ ] **데이터 로드**: 홈 화면에서 전체 단어 수가 `171개`로 정상 표출되는지 확인.
- [ ] **학습 상태**: 단어 목록에서 임의의 단어를 누른 뒤 상세 페이지에서 '외웠어요' 또는 '다시 볼래요'를 탭하고, 홈 화면의 진행률 게이지와 통계 탭에서 수치가 실시간 동기화되는지 확인.
- [ ] **검색 및 필터**: 목록 화면에서 검색어를 입력했을 때 키워드와 한줄설명 영역에 노란색 형광펜 하이라이팅이 잘 입혀지는지, 카테고리 필터와 즐겨찾기 필터가 올바르게 차단/작동하는지 확인.
- [ ] **암기 플래시카드**: 카드를 터치했을 때 3D 회전 애니메이션이 부드럽게 돌아가며 정답(뒷면)이 나타나는지, '알아요/헷갈려요/몰라요' 버튼을 누르면 다음 카드로 자연스럽게 넘어가고 복습노트에 저장되는지 확인.
- [ ] **퀴즈**: 4지선다 보기를 눌렀을 때 초록색(정답)/빨간색(오답) 즉시 피드백이 노출되는지, 퀴즈 종료 시 리포트가 정상 생성되고 틀린 단어들만 오답노트에 반영되는지 확인.
- [ ] **오답노트 퀴즈**: 오답노트에서 '오답 단어로 퀴즈 풀기'를 실행했을 때, 퀴즈에서 정답을 맞힌 단어는 오답노트 목록에서 자동으로 소거되는지 확인.
- [ ] **PWA 동작**: 오프라인 모드(크롬 개발자 도구의 Network -> Offline) 상태로 설정하고 앱 새로고침 시에도 기본 화면 및 단어 데이터 조회가 가능한지 확인.
